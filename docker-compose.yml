services:

  # ─── Container 1: MySQL Database ─────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: meditrack-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: suivi_medical_db
      MYSQL_USER: meditrack
      MYSQL_PASSWORD: meditrack123
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - meditrack-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "meditrack", "-pmeditrack123"]
      interval: 10s
      timeout: 20s
      retries: 10
      start_period: 40s

  # ─── Container 2: MongoDB ─────────────────────────────────────
  mongodb:
    image: mongo:7.0
    container_name: meditrack-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - meditrack-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s
    environment:
      SPRING_DATA_MONGODB_URI: "mongodb://mongodb:27017/suivi_medical_chat"
      SPRING_DATA_MONGODB_DATABASE: suivi_medical_chat  

  # ─── Container 3: Spring Boot Backend ────────────────────────
  backend:
    build:
      context: ./suivi-medical
      dockerfile: Dockerfile
    container_name: meditrack-backend
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/suivi_medical_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: meditrack
      SPRING_DATASOURCE_PASSWORD: meditrack123
      SPRING_DATA_MONGODB_URI: mongodb://mongodb:27017/suivi_medical_chat
      SPRING_DATA_MONGODB_DATABASE: suivi_medical_chat
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      APP_JWT_SECRET: mySecretKey1234567890abcdefghijklmnopqrstuvwxyz
      APP_JWT_EXPIRATION: "86400000"
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      mysql:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    networks:
      - meditrack-network

  # ─── Container 4: React Frontend ─────────────────────────────
  frontend:
    build:
      context: ./medical-app
      dockerfile: Dockerfile
    container_name: meditrack-frontend
    restart: unless-stopped
    ports:
      - "4000:80"
    depends_on:
      - backend
    networks:
      - meditrack-network

# ─── Volumes ──────────────────────────────────────────────────
volumes:
  mysql_data:
  mongo_data:
  uploads_data:

# ─── Network ──────────────────────────────────────────────────
networks:
  meditrack-network:
    driver: bridge